import {WriterComponent} from '../WriterComponent.js';
/*eslint-disable no-unused-vars*/
import React from 'react';
/*eslint-disable no-unused-vars*/
import {shallow, mount} from 'enzyme';
import {expect} from 'chai';
import sinon from 'sinon';

describe('' , function(){
    it('', function(){
        
        // var WrtComp = shallow(<WriterComponent/>);
        // const inputEvent = {target: {value:'45'}, preventDefault: () => {} };
        // WrtComp.instance().handleSubmit(inputEvent);
        // const inputEvent = {target: {value:'45'}, preventDefault: () => {} };
        // const WrtComp = shallow(<WriterComponent/>);
        // const EpicsStub = sinon.stub(WrtComp);

        // WrtComp.find('form').simulate('submit', [inputEvent, EpicsStub]);

        
    });
});